#Задача №1 (Раздел: Объявление переменных и типы данных)

#Создайте три переменные:

#my_name — присвойте ей строковое значение, содержащее ваше имя.
#my_age — присвойте ей целочисленное значение, соответствующее вашему возрасту.
#i_am_new — присвойте ей булево значение, которое будет указывать, что вы новичок в программировании (используйте True или False).
#В конце программы выведите на экран (через print()) тип данных каждой из этих переменных, используя функцию type().

#Решение:
my_name = "Kirill"
my_age = 31
i_am_new = True

print(type(my_name))
print(type(my_age))
print(type(i_am_new))

#Задача №2 (Раздел: Арифметические операции и преобразование типов данных)

#У вас есть две переменные, заданные как строки:
#str_num1 = "15"
#str_num2 = "3"
#Преобразуйте обе строки в целые числа (int) и сохраните результаты в новые переменные.
#Выполните три арифметические операции с преобразованными числами:
#Сложение
#Умножение
#Целочисленное деление (деление с остатком, где результат — целое число)
#Результат каждой операции выведите на экран в виде целого числа.
#Отправьте ваш код.

#Решение:
str_num1 = "15"
str_num2 = "3"

num1 = int(str_num1)
num2 = int(str_num2)

result = num1 + num2
print(result)
result = num1 * num2
print(result)
result = num1 // num2
print(result)

#Задача №3 (Раздел: Операции сравнения и именование переменных)

#Создайте две переменные с правильными именами (следуя правилам именования в Python):
#Первая переменная должна хранить количество студентов в группе (например, 20).
#Вторая переменная должна хранить количество присланных домашних работ (например, 18).
#Используя операции сравнения, проверьте и выведите результат (True или False) для следующих условий:
#Количество студентов больше количества работ.
#Количество работ меньше или равно количеству студентов.
#Количество студентов не равно количеству работ.

#Решение:
total_students = 20
total_home_works = 18

print(total_students>total_home_works)
print(total_home_works<=total_students)
print(total_students!=total_home_works)

#Задача №4 (Раздел: Преобразование типов данных и арифметические операции)

#У вас есть три переменные:
#a = 10 (целое число)
#b = "5.7" (строка, представляющая дробное число)
#c = True (булево значение)

#Преобразуйте переменную b в число с плавающей точкой (float).
#Преобразуйте переменную c в целое число (int). Вспомните, чему равны int(True) и int(False).
#Вычислите и выведите на экран результат выражения: a + преобразованное_b + преобразованное_c

#Решение:
a = 10
b = "5.7"
c = True

b_float = float(b)
c_int = int(c)
result = (a+b_float+c_int)

print(result)

#Задача №5 (Раздел: Переопределение переменной)

#Создайте переменную с именем total и присвойте ей значение 100.
#Выведите текущее значение total на экран.
#Переопределите переменную total, уменьшив её значение на 15 (используя короткую запись операции присваивания, например, +=, -=, *=, /=).
#Снова выведите значение total на экран.

#Решение:
total = 100
print(total)
total -=  15
print(total)

#Задача №6 (Раздел: Типы данных и преобразование)

#Создайте переменную data и присвойте ей значение "12345".
#Проверьте и выведите на экран тип данных этой переменной, используя функцию type().
#Преобразуйте значение переменной data в целое число (int) и сохраните результат обратно в ту же переменную data (переопределите её).
#Снова проверьте и выведите на экран тип данных переменной data.

#Решение:
data = "12345"

print(type(data))

data = int(data)

print(type(data))

#Задача №7 (Раздел: Классификация по типам данных)

#У вас есть список разных значений:
#my_data = [42, 3.14, "Hello", True, None]
#Пройдитесь по каждому элементу этого списка (например, с помощью цикла for или вручную по индексу).
#Для каждого элемента определите и выведите на экран, к какому базовому/скалярному типу данных в Python он относится. В вашем ответе должны фигурировать только названия типов: int, float, str, bool, NoneType.
#Подсказка: для типа None функция type() вернёт <class 'NoneType'>.

#Решение:
my_data = [42, 3.14, "Hello", True, None]

element_0 = my_data[0]
element_1 = my_data[1]
element_2 = my_data[2]
element_3 = my_data[3]
element_4 = my_data[4]

print(type(element_0))
print(type(element_1))
print(type(element_2))
print(type(element_3))
print(type(element_4))

#Задача №8 (Повторение: Арифметические операции, преобразование типов, переопределение)

#У вас есть три переменные:
#price = "150" (строка)
#quantity = 4 (целое число)
#discount = 10 (целое число, скидка в процентах)

#Преобразуйте переменную price в целое число.
#Рассчитайте общую стоимость без скидки: price * quantity. Сохраните результат в переменную total.
#Рассчитайте размер скидки в денежных единицах по формуле: (total * discount) / 100. Сохраните результат в переменную discount_amount.
#Переопределите переменную total, вычтя из неё размер скидки (discount_amount).
#Выведите на экран три значения (каждое с новой строки):
#Общую стоимость без скидки
#Размер скидки
#Итоговую стоимость со скидкой

#Решение:

price = "150"
quantity = 4
discount = 10

price = int(price)
total_without_discount = price * quantity

discount_amount = (total_without_discount * discount) / 100

total_with_discount = total_without_discount - discount_amount

print(total_without_discount)
print(discount_amount)
print(total_with_discount)


#Задача №9 (Повторение: операции сравнения, преобразование типов, именование)

#Создайте две переменные:

#hours_str — строку со значением "40" (количество часов).
#rate — целое число со значением 500 (ставка в час).
#Преобразуйте hours_str в целое число и сохраните в переменную hours_int.
#Рассчитайте заработную плату по формуле: hours_int * rate. Сохраните результат в переменную salary.
#Используя операции сравнения, проверьте и выведите результат (True или False) для двух условий:
#Заработная плата (salary) больше 15000.
#Количество часов (hours_int) не равно rate

#Решение:

hours_str = "40"
rate =  500

hours_int = int(hours_str)

salary = hours_int * rate

print(salary > 15000)
print(hours_int != rate)

#Задача №10 (Повторение: базовые типы данных, арифметика, вывод)

#Создайте четыре переменных, каждая из которых представляет собой один из базовых типов данных Python:
#Целое число (например, ваш любимый номер)
#Число с плавающей точкой (например, математическая константа Пи с двумя знаками после запятой — 3.14)
#Строку (например, ваше любимое слово)
#Булево значение (например, False)
#Выполните одну арифметическую операцию, используя только числовые переменные (целое и дробное). Операцию выберите сами (сложение, вычитание, умножение или деление). Результат сохраните в переменную calc_result.
#Выведите на экран в столбик (каждое с новой строки):
#Значение каждой из четырёх созданных переменных.
#Результат арифметической операции (calc_result).
#Отправьте ваш код.

#Решение:
num = 25
num2 = 3.14
string = "bonofide"
boolean = False

calc_result = num + num2

print(num)
print(num2)
print(string)
print(boolean)
print(calc_result)

#Задача №11 (Раздел: Объявление переменных и типы данных)

#Создайте три переменные:
#city — присвойте ей строковое значение с названием вашего города.
#population — присвойте ей целочисленное значение, примерно соответствующее населению этого города.
#is_capital — присвойте ей булево значение, указывающее, является ли этот город столицей (True или False).
#В конце программы выведите на экран (каждое с новой строки) тип данных каждой из этих переменных, используя функцию type().

#Решение:

city = "Stary Oskol"
population = 250000
is_capital = False

print(type(city))
print(type(population))
print(type(is_capital))

#Задача №12 (Раздел: Арифметические операции и преобразование типов данных)

#У вас есть две переменные:
#str_a = "100"
#str_b = "8"

#Преобразуйте обе строки в целые числа (int) и сохраните результаты в новые переменные.
#Выполните три арифметические операции с преобразованными числами:
#Вычитание
#Обычное деление (результат будет дробным)
#Остаток от деления (модуль)
#Результат каждой операции выведите на экран (каждый с новой строки).

#Решение:

str_a = "100"
str_b = "8"

str_a_int = int(str_a)
str_b_int = int(str_b)

minus = str_a_int - str_b_int
division = str_a_int / str_b_int
result = str_a_int % str_b_int

print(minus)
print(division)
print(result)

#Задача №13 (Раздел: Переопределение переменной)

#Создайте переменную с именем counter и присвойте ей значение 50.
#Выведите текущее значение counter на экран.
#Переопределите переменную counter, увеличив её значение на 7 (используя короткую запись операции присваивания, например, +=).
#Снова выведите значение counter на экран.

#Решение:

counter = 50
print(counter)

counter += 7
print(counter)

#Задача №14 (Раздел: Операции сравнения и именование переменных)

#Создайте две переменные с правильными именами:
#Первая должна хранить температуру в помещении (например, 22).
#Вторая должна хранить желаемую температуру (например, 24).
#Используя операции сравнения, проверьте и выведите результат (True или False) для следующих условий (каждый с новой строки):
#Температура в помещении меньше желаемой.
#Температура в помещении не равна желаемой.
#Желаемая температура больше или равна температуре в помещении.

#Решение:

temperature_in_house = 22
temperature_like = 24

print(temperature_in_house < temperature_like)
print(temperature_in_house != temperature_like)
print(temperature_like >= temperature_in_house)

# Задача №15 (Раздел: Преобразование типов данных)
#
# У вас есть три переменные:
# x = "15.5" (строка, представляющая дробное число)
# y = 4 (целое число)
# z = False (булево значение)

# Преобразуйте переменную x в число с плавающей точкой (float).
# Преобразуйте переменную z в целое число (int).
# Вычислите и выведите на экран результат выражения: преобразованный_x * y - преобразованный_z

#Решение:

x = "15.5"
y = 4
z = False

x_float = float(x)
z_int = int(z)

result = x_float * y - z_int
print(result)

