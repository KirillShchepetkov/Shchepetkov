# Задача №1 по спискам (Раздел: Создание списка и получение элементов)
#
# Создайте список с именем protocols, содержащий 5 строковых элементов —
# названия сетевых протоколов (например, "HTTP", "HTTPS", "FTP", "SSH", "TCP").
#
# Элементы задайте вручную при создании списка.
# Выведите на экран (каждый с новой строки):
# Весь список целиком.
# Первый элемент списка.
# Третий элемент списка.
# Последний элемент списка (используя отрицательный индекс).

#Решение:

protocols = ["HTTP", "HTTPS", "FTP", "SSH", "TCP"]
print(protocols)

first_protocol = protocols[0]
print(first_protocol)

third_protocol = protocols[2]
print(third_protocol)

last_protocol = protocols[-1]
print(last_protocol)

# Задача №2 по спискам (Раздел: Добавление элементов в список и их замена)
#
# У вас есть начальный список:
# errors = [404, 500, 401]
#
# Добавьте в конец списка новый код ошибки 502 (используя метод).
# Замените второй элемент списка (текущее значение 500) на код 408.
# Добавьте в начало списка новый код ошибки 403 (используя метод).
# Выведите итоговый список на экран.

#Решение:

errors = [404, 500, 401]

errors.append(502)

errors[1] = 408

errors.insert(0, 403)

print(errors)

# Задача №3 по спискам (Раздел: Удаление элементов из списка и его очистка)
#
# У вас есть список:
# tools = ["Selenium", "Playwright", "Cypress", "Appium", "Selenium", "TestComplete"]
#
# Удалите из списка элемент "Appium" (используя метод удаления по значению).
# Удалите из списка элемент с индексом 2 (используя метод удаления по индексу).
# Удалите из списка последний элемент (используя метод без указания индекса).
# Очистите список полностью (удалите все оставшиеся элементы).
# Выведите на экран итоговый список после всех операций.

#Решение:

tools = ["Selenium", "Playwright", "Cypress", "Appium", "Selenium", "TestComplete"]

tools.remove("Appium")

del tools[2]

tools.pop()

tools.clear()

print(tools)

# Задача №4 по спискам (Раздел: Срезы списка)
#
# У вас есть список:
# versions = ["1.0", "1.1", "2.0", "2.1", "3.0", "3.1", "4.0"]
#
# Используя срезы (slice), создайте и сохраните в новые переменные следующие подсписки:
#
# Первые три версии.
# Последние три версии.
# Версии с третьей по пятую включительно (считая с 1).
# Каждую вторую версию, начиная с первой.
# Список в обратном порядке.
# Выведите на экран (каждый с новой строки) все полученные подсписки, а также исходный список.

#Решение:

versions = ["1.0", "1.1", "2.0", "2.1", "3.0", "3.1", "4.0"]

slice_1 = versions[:3]
print(slice_1)

slice_2 = versions[-3:]
print(slice_2)

slice_3 = versions[2:5]
print(slice_3)

slice_4 = versions[::2]
print(slice_4)

slice_5 = versions[::-1]
print(slice_5)

print(versions)

# Задача №5 по спискам (Раздел: Слияние списков)
#
# У вас есть два списка:
# team_a = ["Анна", "Борис", "Виктор"]
# team_b = ["Галина", "Дмитрий", "Елена"]
#
# Создайте новый список all_team, который является слиянием двух списков (team_a и team_b) с помощью оператора +.
# С помощью метода добавьте в конец списка all_team нового сотрудника "Жанна".
# С помощью метода расширения добавьте к списку all_team список
# interns = ["Игорь", "Ксения"] (т.е. добавьте эти два имени как отдельные элементы в конец).
#
# Выведите на экран:
# Итоговый список all_team.
# Длину итогового списка (количество элементов).

#Решение:

team_a = ["Анна", "Борис", "Виктор"]
team_b = ["Галина", "Дмитрий", "Елена"]

all_team = team_a + team_b

all_team.append("Жанна")

interns = ["Игорь", "Ксения"]

all_team.extend(interns)

length = len(all_team)

print(all_team)
print(length)

# Задача №6 по спискам (Раздел: Получение элементов списка и добавление в конкретное место)
#
# У вас есть список:
# status_codes = [200, 201, 400, 401, 403, 500]
#
# Получите и сохраните в переменные:
# Второй элемент списка.
# Предпоследний элемент списка (используя отрицательный индекс).
# Элемент с индексом 3.
#
# Добавьте код 404 в список после элемента 403 (т.е. на позицию, следующую за 403).
# Найдите индекс элемента 403 и используйте метод добавления по индексу.
#
# Добавьте код 301 в список перед элементом 400 (используя метод добавления по индексу).
# Выведите на экран:
# Итоговый список.
# Все три сохранённые переменные из первого пункта (каждую с новой строки).

#Решение:

status_codes = [200, 201, 400, 401, 403, 500]

variables_1 = status_codes[1]

variables_2 = status_codes[-2]

variables_3 = status_codes[3]

index_403 = status_codes.index(403)
status_codes.insert(index_403 + 1, 404)

status_codes.insert(2, 301)

print(status_codes)
print(variables_1)
print(variables_2)
print(variables_3)

# Задача №7 по спискам (Раздел: Замена элементов в списке)
#
# У вас есть список уровней логирования:
# log_levels = ["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL", "INFO", "DEBUG"]
#
# Замените первый элемент списка на "TRACE".
#
#Замените последний элемент списка на "FATAL".
#
# Найдите индекс второго вхождения элемента "INFO" в списке и замените этот элемент на "NOTICE".
# Используйте метод для поиска индекса с параметром start, чтобы найти именно второе вхождение.
#
# Выведите на экран итоговый список.

#Решение:

log_levels = ["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL", "INFO", "DEBUG"]

log_levels[0] = "TRACE"

log_levels[-1] = "FATAL"

first_index = log_levels.index("INFO")
second_index = log_levels.index("INFO", first_index + 1)

log_levels[second_index] = "NOTICE"

print(log_levels)

# Задача №8 по спискам (Раздел: Добавление элементов в конкретное место списка)
#
# У вас есть список шагов тест-кейса:
# test_steps = ["Открыть браузер", "Ввести URL", "Ввести логин", "Нажать кнопку Войти"]
#
# Добавьте шаг "Закрыть браузер" в конец списка.
# Добавьте шаг "Проверить заголовок страницы" после шага "Ввести URL" (используйте поиск индекса и метод вставки).
# Добавьте шаг "Очистить кэш" в начало списка.
# Добавьте шаг "Ввести пароль" перед шагом "Нажать кнопку Войти" (используйте поиск индекса и метод вставки).
#
# Выведите на экран:
# Итоговый список.
# Количество шагов в итоговом списке (длину списка).

#Решение:

test_steps = ["Открыть браузер", "Ввести URL", "Ввести логин", "Нажать кнопку Войти"]

test_steps.append("Закрыть браузер")

test_steps_index = test_steps.index("Ввести URL")
test_steps.insert(test_steps_index + 1, "Проверить заголовок страницы")


test_steps.insert(0, "Очистить кэш")

test_steps_index_3 = test_steps.index("Нажать кнопку Войти")
test_steps.insert(test_steps_index_3 + 1, "Ввести пароль")


print(test_steps)
length = len(test_steps)
print(length)

# Задача №9 по спискам (Раздел: Слияние списков и срезы)
#
# У вас есть два списка:
# frontend_tools = ["React", "Vue", "Angular"]
# backend_tools = ["Django", "Flask", "FastAPI"]
#
# Создайте новый список web_stack,
# который является слиянием списка frontend_tools
# и списка backend_tools с помощью метода .extend() (измените первый список, добавив в него второй).
#
# Используя срезы, создайте и сохраните в переменные:
# Подсписок из первых трёх элементов web_stack.
# Подсписок из последних двух элементов web_stack.
# Подсписок, содержащий каждый второй элемент web_stack, начиная со второго.
# Добавьте в начало списка web_stack элемент "HTML/CSS" (используя метод).
# Выведите на экран (каждый с новой строки):
# Итоговый список web_stack.
# Все три подсписка, полученные срезами.

#Решение:

frontend_tools = ["React", "Vue", "Angular"]
backend_tools = ["Django", "Flask", "FastAPI"]

frontend_tools.extend(backend_tools)
web_stack = frontend_tools

web_stack_3 = web_stack[2:]
print(web_stack_3)

web_stack_last2 = web_stack[-2:]
print(web_stack_last2)

web_stack_second = web_stack[2::2]
print(web_stack_second)

web_stack.insert(0, "HTML/CSS")
print(web_stack)

# Задача №10 по третьей теме (Раздел: Создание списка и получение элементов)
#
# Создайте список с именем browsers,
# содержащий 4 строковых элемента — названия браузеров (например, "Chrome", "Firefox", "Safari", "Edge").
# Элементы задайте вручную при создании списка.
#
# Выведите на экран (каждый с новой строки):
# Весь список целиком.
# Первый элемент списка.
# Третий элемент списка.
# Последний элемент списка (используя отрицательный индекс).

#Решение:
browsers = ["Chrome", "Firefox", "Safari", "Edge"]
print(browsers)

first_browsers = browsers[0]
print(first_browsers)

third_protocol = browsers[2]
print(third_protocol)

last_protocol = browsers[-1]
print(last_protocol)

# Задача №11 по спискам (Раздел: Создание списка и получение элементов)
#
# Вручную создайте список с именем frameworks, содержащий 4 строковых элемента
# — названия фреймворков для автоматизации (например, "Selenium", "Playwright", "Cypress", "Appium").
#
# Выведите на экран (каждое с новой строки):
#
# Весь список целиком.
# Первый элемент списка.
# Последний элемент списка (используя отрицательный индекс).

#Решение:
frameworks = ["Selenium", "Playwright", "Cypress", "Appium"]

print(frameworks)
first_frameworks = frameworks[0]
print(first_frameworks)
last_frameworks = frameworks[-1]
print(last_frameworks)

# Задача №12 (третья тема: Списки)
#
# Вручную создайте список с именем environments, содержащий 4 строковых элемента —
# названия сред тестирования (например, "dev", "test", "staging", "prod").
#
# Выведите на экран (каждое с новой строки):
# Весь список целиком.
# Первый элемент списка.
# Последний элемент списка (используя отрицательный индекс).
# Длину списка (количество элементов).

#Решение:

environments = ["dev", "test", "staging", "prod"]

print(environments)

first_environments = environments[0]
print(first_environments)

last_environments = environments[-1]
print(last_environments)

length = len(environments)
print(length)